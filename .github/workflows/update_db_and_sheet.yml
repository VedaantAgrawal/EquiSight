name: update-db-and-sheet-15m

on:
  schedule:
    # Every 15 min between 14:30–21:00 UTC (9:30 AM–4:00 PM ET), Mon–Fri
    - cron: "*/15 14-20 * * 1-5"
    - cron: "30 21 * * 1-5"   # final run after close
  workflow_dispatch: {}

concurrency:
  group: db-and-sheet
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt

      - name: Update Neon DB with latest bars
        env:
          DB_URL: ${{ secrets.DB_URL }}
          ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
          ALPACA_API_SECRET: ${{ secrets.ALPACA_API_SECRET }}
          ALPACA_DATA_FEED: ${{ secrets.ALPACA_DATA_FEED }}
          SYMBOLS: ${{ secrets.SYMBOLS }}
        run: python update.py

      - name: Update Google Sheet with today's data
        env:
          DB_URL: ${{ secrets.DB_URL }}
          GCP_SERVICE_ACCOUNT: ${{ secrets.GCP_SERVICE_ACCOUNT }}
          SHEET_ID: ${{ secrets.SHEET_ID }}
        run: python sheet_update.py
